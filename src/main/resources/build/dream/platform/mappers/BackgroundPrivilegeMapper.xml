<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.BackgroundPrivilegeMapper">
    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.BackgroundPrivilege">
        SELECT ${selectColumns} FROM background_privilege <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.BackgroundPrivilege">
        SELECT ${selectColumns} FROM background_privilege <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>
</mapper>