<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.AppPrivilegeMapper">
    <select id="findAllAppPrivileges" resultType="build.dream.common.domains.saas.AppPrivilege">
        SELECT
        *
        FROM app_privilege
        WHERE id IN (SELECT privilege_id FROM app_role_privilege_r WHERE app_role_id IN (SELECT app_role_id FROM user_app_role_r WHERE user_id = #{userId}))
        AND deleted = 0
    </select>
</mapper>