<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.OrderDetailMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.OrderDetail" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_detail(order_id, goods_id, goods_specification_id,
        branch_id, price, discount_amount,
        payable_amount, amount, create_user_id,
        last_update_user_id, last_update_remark)
        VALUES (#{orderId}, #{goodsId}, #{goodsSpecificationId},
        #{branchId}, #{price}, #{discountAmount},
        #{payableAmount}, #{amount}, #{createUserId},
        #{lastUpdateUserId}, #{lastUpdateRemark})
    </insert>

    <insert id="insertAll" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_detail(order_id, goods_id, goods_specification_id,
        branch_id, price, discount_amount,
        payable_amount, amount, create_user_id,
        last_update_user_id, last_update_remark)
        VALUES
        <foreach collection="list" index="index" item="orderDetail" separator=",">
            (#{orderDetail.orderId}, #{orderDetail.goodsId}, #{orderDetail.goodsSpecificationId},
            #{orderDetail.branchId}, #{orderDetail.price}, #{orderDetail.discountAmount},
            #{orderDetail.payableAmount}, #{orderDetail.amount}, #{orderDetail.createUserId},
            #{orderDetail.lastUpdateUserId}, #{orderDetail.lastUpdateRemark})
        </foreach>
    </insert>
</mapper>