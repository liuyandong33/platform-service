<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.TenantMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.Tenant" useGeneratedKeys="true" keyProperty="id">
        ${@build.dream.common.utils.BeanUtils@generateInsertSql("build.dream.common.saas.domains.Tenant")}
    </insert>

    <update id="update" parameterType="build.dream.common.saas.domains.SystemUser">
        ${@build.dream.common.utils.BeanUtils@generateUpdateSql("build.dream.common.saas.domains.Tenant")}
    </update>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAllPaged" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include> <include refid="build.dream.platform.mappers.CommonMapper.paginationSql"></include>
    </select>

    <insert id="insertAllBranchInfos" useGeneratedKeys="false">
        INSERT INTO ${partitionCode}_branch_info(id, code, name, type, status, tenant_id)
        VALUES
        <foreach collection="branchInfos" index="index" item="branchInfo" separator=",">
            (#{branchInfo.id}, #{branchInfo.code}, #{branchInfo.name}, #{branchInfo.type}, #{branchInfo.status}, #{branchInfo.tenantId})
        </foreach>
    </insert>
</mapper>