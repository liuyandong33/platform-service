<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.TenantMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.Tenant" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tenant(code, name, mobile,
        email, linkman, business,
        province_code, city_code, district_code,
        partition_code, user_id, tenant_type,
        create_user_id, last_update_user_id, last_update_remark)
        VALUES (#{code}, #{name}, #{mobile},
        #{email}, #{linkman}, #{business},
        #{provinceCode}, #{cityCode}, #{districtCode},
        #{partitionCode}, #{userId}, #{tenantType},
        #{createUserId}, #{lastUpdateUserId}, #{lastUpdateRemark})
    </insert>

    <update id="update" parameterType="build.dream.common.saas.domains.SystemUser">
        UPDATE tenant
        SET code = #{code},
            name = #{name},
            mobile = #{mobile},
            email = #{email},
            linkman = #{linkman},
            business = #{business},
            province_code = #{provinceCode},
            city_code = #{cityCode},
            district_code = #{districtCode},
            partition_code = #{partitionCode},
            user_id = #{userId},
            tenant_type = #{tenantType},
            create_user_id = #{createUserId},
            last_update_user_id = #{lastUpdateUserId},
            last_update_remark = #{lastUpdateRemark},
            deleted = #{deleted}
        WHERE id = #{id}
    </update>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant <include refid="searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant <include refid="searchConditionSql"></include>
    </select>

    <select id="findAllPaged" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant <include refid="searchConditionSql"></include> <include refid="paginationSql"></include>
    </select>

    <sql id="searchConditionSql">
        WHERE
        <foreach collection="searchConditions" index="index" item="searchCondition" separator=" AND ">
            <choose>
                <when test="@org.apache.commons.lang.StringUtils@equalsIgnoreCase(searchCondition.operationSymbol, 'IN')">
                    ${searchCondition.columnName} ${searchCondition.operationSymbol}
                    <foreach collection="searchCondition.searchParameter" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </when>
                <otherwise>
                    ${searchCondition.columnName} ${searchCondition.operationSymbol} #{searchCondition.searchParameter}
                </otherwise>
            </choose>
        </foreach>
    </sql>

    <sql id="paginationSql">
        LIMIT #{offset}, #{maxResults}
    </sql>

    <select id="findById" resultType="build.dream.common.saas.domains.Tenant">
        SELECT * FROM tenant WHERE id = #{tenantId} AND deleted = 0
    </select>
</mapper>