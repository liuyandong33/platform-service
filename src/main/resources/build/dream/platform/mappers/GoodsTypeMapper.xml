<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.GoodsTypeMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.GoodsType" useGeneratedKeys="true" keyProperty="id">
        ${@build.dream.common.utils.DatabaseUtils@generateInsertSql("build.dream.common.saas.domains.GoodsType")}
    </insert>

    <update id="update" parameterType="build.dream.common.saas.domains.GoodsType">
        ${@build.dream.common.utils.DatabaseUtils@generateUpdateSql("build.dream.common.saas.domains.GoodsType")}
    </update>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.GoodsType">
        SELECT ${selectColumns} FROM goods_type <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.GoodsType">
        SELECT ${selectColumns} FROM goods_type <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="count" parameterType="build.dream.common.utils.SearchModel" resultType="long">
        SELECT COUNT(1) FROM goods_type <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAllPaged" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.GoodsType">
        SELECT ${selectColumns} FROM goods_type <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include> <include refid="build.dream.platform.mappers.CommonMapper.paginationSql"></include>
    </select>
</mapper>