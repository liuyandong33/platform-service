<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.ElemeCallbackMessageMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.ElemeCallbackMessage" useGeneratedKeys="true" keyProperty="id">
        ${@build.dream.common.utils.BeanUtils@generateInsertSql("build.dream.common.saas.domains.ElemeCallbackMessage")}
    </insert>

    <update id="update" parameterType="build.dream.common.saas.domains.ElemeCallbackMessage">
        ${@build.dream.common.utils.BeanUtils@generateUpdateSql("build.dream.common.saas.domains.ElemeCallbackMessage")}
    </update>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.ElemeCallbackMessage">
        SELECT ${selectColumns} FROM eleme_callback_message <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.ElemeCallbackMessage">
        SELECT ${selectColumns} FROM eleme_callback_message <include refid="build.dream.platform.mappers.CommonMapper.searchConditionSql"></include>
    </select>
</mapper>