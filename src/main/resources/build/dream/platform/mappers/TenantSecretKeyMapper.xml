<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.TenantSecretKeyMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.TenantSecretKey" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tenant_secret_key(tenant_id, tenant_code, public_key,
        private_key, create_user_id, last_update_user_id,
        last_update_remark)
        VALUES(#{tenantId}, #{tenantCode}, #{publicKey},
        #{privateKey}, #{createUserId}, #{lastUpdateUserId},
        #{lastUpdateRemark})
    </insert>

    <update id="update" parameterType="build.dream.common.saas.domains.TenantSecretKey">
        UPDATE tenant_secret_key
        SET tenant_id = #{tenantId},
            tenant_code = #{tenantCode},
            public_key = #{publicKey},
            private_key = #{privateKey},
            create_user_id = #{createUserId},
            last_update_user_id = #{lastUpdateUserId},
            last_update_remark = #{lastUpdateRemark},
            deleted = #{deleted}
        WHERE id = #{id}
    </update>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.TenantSecretKey">
        SELECT * FROM tenant_secret_key <include refid="searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.saas.domains.TenantSecretKey">
        SELECT * FROM tenant_secret_key <include refid="searchConditionSql"></include>
    </select>

    <sql id="searchConditionSql">
        WHERE
        <foreach collection="searchConditions" index="index" item="searchCondition" separator=" AND ">
            <choose>
                <when test="@org.apache.commons.lang.StringUtils@equalsIgnoreCase(searchCondition.operationSymbol, 'IN')">
                    ${searchCondition.columnName} ${searchCondition.operationSymbol}
                    <foreach collection="searchCondition.searchParameter" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </when>
                <otherwise>
                    ${searchCondition.columnName} ${searchCondition.operationSymbol} #{searchCondition.searchParameter}
                </otherwise>
            </choose>
        </foreach>
    </sql>
</mapper>