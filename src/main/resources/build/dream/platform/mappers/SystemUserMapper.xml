<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.SystemUserMapper">
    <insert id="insert" parameterType="build.dream.common.saas.domains.SystemUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_user(name, mobile, email,
        login_name, user_type, password,
        tenant_id, account_non_expired, account_non_locked,
        credentials_non_expired, enabled, create_user_id,
        last_update_user_id)
        VALUES (#{name}, #{mobile}, #{email},
        #{loginName}, #{userType}, #{password},
        #{tenantId}, #{accountNonExpired}, #{accountNonLocked},
        #{credentialsNonExpired}, #{enabled}, #{createUserId},
        #{lastUpdateUserId})
    </insert>

    <update id="update" parameterType="build.dream.common.saas.domains.SystemUser">
        UPDATE system_user
        SET name = #{name}, mobile = #{mobile}, email = #{email},
            login_name = #{loginName}, user_type = #{userType}, password = #{password},
            tenant_id = #{tenantId}, account_non_expired = #{accountNonExpired}, account_non_locked = #{accountNonLocked},
            credentials_non_expired = #{credentialsNonExpired}, enabled = #{enabled}, create_user_id = #{createUserId},
            last_update_user_id = #{lastUpdateUserId}
        WHERE id = #{id}
    </update>

    <select id="findByLoginNameOrEmailOrMobile" resultType="build.dream.common.saas.domains.SystemUser">
        SELECT * FROM system_user WHERE (login_name = #{loginName} OR email = #{loginName} OR mobile = #{loginName}) AND deleted = 0
    </select>
</mapper>