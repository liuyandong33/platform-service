<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.platform.mappers.PosPrivilegeMapper">
    <select id="findAllPosPrivileges" resultType="build.dream.common.domains.saas.PosPrivilege">
        SELECT
        *
        FROM pos_privilege
        WHERE id IN (SELECT privilege_id FROM pos_role_privilege_r WHERE pos_role_id IN (SELECT pos_role_id FROM user_pos_role_r WHERE user_id = #{userId}))
        AND deleted = 0
    </select>
</mapper>